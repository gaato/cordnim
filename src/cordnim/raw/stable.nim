## Stable Discord HTTP v10 models, routes, and compatibility metadata.
##
## Import this module to use the generated standard API snapshot without
## preview operations. Edit the pinned schema inputs and run the generator when
## the upstream specification changes; generated declarations are not manual
## extension points.

import ./[model, models, request, route, routes, schema_info]

export model, models, request, route, routes, schema_info
